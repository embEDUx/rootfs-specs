---
packages:
    app-emulation/docker:
        kw:
            +: "~arm"
        use:
            +: "btrfs contrib doc"
            -: "device-mapper"

post_install_command:
  - "rc-update add docker default"
