---
global:
    use:
      +: "vim-syntax"
      -: "X doc"

packages:
  net-misc/x2goclient: {}

pre_install_commands:
  - "emerge --sync"

post_install_commands:
  - "echo Europe/Berlin > /etc/timezone"
  - "emerge --config timezone-data"
