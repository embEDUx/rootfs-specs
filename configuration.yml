---
global:
    kw:
      +: ["~arm"]
    use:
      -: ["X","doc"]

packages:
  app-emulation/docker:
    use:
      +: ["btrfs","doc"]
  app-editors/vim: {}
  net-misc/ntp:
    use:
      +: ["vim-syntax"]
  